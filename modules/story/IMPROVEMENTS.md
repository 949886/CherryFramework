# Story 模块完善进度

按顺序实施并分别提交；测试使用 Godot 4.7.2 .NET 版的独立项目。

1. 已完成：解析器严格结构校验、明确剧情 ID / 本地化文件映射、可读取的源文件诊断。`parser_test.gd` 20 项、原有 146 项通过。
2. 已完成：深快照、版本/结构/类型校验、事务恢复、有界迁移、原子文件替换。`save_test.gd` 32 项、原有 146 项通过。
3. 已完成：画面与行内进度恢复、统一暂停/继续/自动播放/快进时间控制。`presentation_test.gd` 31 项与原有 146 项通过，覆盖 JSON 句中恢复、语音/立绘/弹图与 60/120 Hz 一致性。
4. 已完成：独立表现层契约、可配置节点引用、行内命令注册与自定义处理器。`extension_test.gd` 15 项覆盖无 UI 播放器、自定义命令、参数校验和状态隔离；原有场景完整播放回归通过。
5. 已完成：内容与编译器版本失效、LRU 容量限制、共享编译产物与独立运行时。`cache_test.gd` 16 项验证命中、源码/映射/版本变化、实例隔离、初始化次数、淘汰和失败恢复；解析器及原有场景回归通过。
6. 已完成：一键库检查与源行预览、Inspector 入口、素材/跳转/翻译 SID 校验、命令注册表统一、自动导出动态依赖及运行时清单。`validator_test.gd` 21 项；编辑器注册/卸载、新节点场景依赖保存、选定场景 PCK 在空目录中完整播放、未选库排除及错误导出诊断通过。

完整运行时回归共 281 项。导出验证使用 Windows Desktop PCK 与兼容渲染器，不要求安装导出模板；导出日志有错误时 Godot 仍可能写出文件，不能仅以 PCK 存在判断成功。

完成证据与接口说明随每个 milestone 更新在此文件及对应测试中。
